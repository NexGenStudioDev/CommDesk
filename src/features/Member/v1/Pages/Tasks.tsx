import PagePlaceholder from "../Components/PagePlaceholder";

export default function TasksPage() {
  return (
    <PagePlaceholder
      title="Tasks"
      description="Manage assigned tasks, deadlines, priorities, and workflow progress."
    />
  );
}
